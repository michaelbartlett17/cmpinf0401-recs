public class CT extends Truck { // Make sure to extend Truck, since it extends Truck, make sure to make Truck's datafields make Truck's datafields protected.
    // Add datafield of type boolean named isCombination to indicate if this is a combinational vehicle

    // Add setter and getters for the datafield

    // Modify the stats method to reflect the new field
}
